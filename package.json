{
  "name": "di08-invoices",
  "version": "1.0.0",
  "description": "Production-ready service for generating Swiss QR-bill compliant PDFs from Harvest invoice data",
  "main": "src/cli/generate-invoice.js",
  "scripts": {
    "dev": "nodemon src/cli/generate-invoice.js -- --id 12345",
    "build": "echo 'no build step needed'",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "gen:invoice": "node src/cli/generate-invoice.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "keywords": [
    "invoice",
    "harvest",
    "swiss-qr-bill",
    "pdf",
    "node.js"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "dayjs": "^1.11.10",
    "dotenv": "^16.3.1",
    "marked": "^16.3.0",
    "puppeteer": "^21.5.0",
    "swissqrbill": "^4.2.0"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.1",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/test/**/*.test.js"
    ]
  }
}
